# Test profile - uses vrstest database
# Create with: CREATE DATABASE vrstest;
spring:
  datasource:
    url: jdbc:postgresql://bastet:5432/vrstest
    username: adrian
    password: Beds1tter

jwt:
  secret: vrs-jwt-secret-key-must-be-at-least-32-chars-long-for-hmac-sha
